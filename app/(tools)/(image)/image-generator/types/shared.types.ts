/**
 * Shared types for Image Generation
 * Extracted to break circular dependency between store and api.types
 */

/**
 * Active tab type - defines the available generation modes
 */
export type ActiveTab = "text-to-image" | "image-reference" | "restyle";

